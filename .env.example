# OpenAI API Key (erforderlich)
# Erhalte deinen API Key unter: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-api-key-here

# Backend Konfiguration (optional, Standardwerte werden verwendet)
# NODE_ENV=development
# PORT=4000

# Datenbank Konfiguration (wird automatisch von Docker gesetzt)
# DB_HOST=db
# DB_PORT=5432
# DB_NAME=voice_agent
# DB_USER=postgres
# DB_PASSWORD=postgres

# CORS – erlaubte Frontend-Origins (kommagetrennt). Standard: FRONTEND_URL.
# Für Produktion z.B.: http://localhost:3000,https://ptw-audio-intelligence.vercel.app
# CORS_ORIGINS=http://localhost:3000,https://your-frontend.vercel.app

# Uploads – Verzeichnis für Audio-Uploads. Standard: backend/uploads
# Render mit persistentem Disk (Mount /var/data): UPLOADS_DIR=/var/data/uploads
# UPLOADS_DIR=/var/data/uploads

# SMTP E-Mail Konfiguration (optional)
SMTP_FROM_EMAIL=noreply@example.com
SMTP_FROM_NAME=Voice Agent
SMTP_HOST=smtp.example.com
SMTP_PASSWORD=your-smtp-password
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=your-email@example.com
